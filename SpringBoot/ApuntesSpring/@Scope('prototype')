Se usa @Scope("prototype") debajo de la linea de @Component para darle un scope de prototype, es decir para que distintas instancias del mismo bean apunten a distintos objetos en memoria,
ya que por defecto spring es singleton