  En clase validaciones.ts donde estan las constantes:
  
  export const nombreApellidoPattern: string ='([a-zA-Z]+) ([a-zA-Z]+)'; //REGEX de la a a la z y de las A a la Z cualquier cantidad de caracteres para la primera palabra, espacio y luego de la a a la z y de las A a la Z cualquier cantidad de caracteres para la segunda palabra
  export const emailPattern         : string = "^[a-z0-9._%+-]+@[a-z0-9.-]+\\.[a-z]{2,4}$";
  
  
  En clase ts donde queremos ocupar las constantes:
  
  import {nombreApellidoPattern,emailPattern, noPuedeSerStrider} from '../../../shared/validators/validaciones'